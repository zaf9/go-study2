1. 进入前端目录，启动前端(npm run dev)
2. 进入后端目录，启动后端(go run main.go -d)
3. 使用chrome devtools mcp连接http://localhost:3000/。注意因为前端的url需要构建，所以等待比较长的时间是正常的，可能需要60-300秒, 所以需要等待一段时间再连接。
账号/密码：admin/GoStudy@789
4. 逐个检查49个章节的测试题目，避免在测试的题目中出现出现 A. B. C. D. 等选项，与真正测验的A B C D 等选项重复，造成混淆。
5. 同时做一些题目验证一下，我发现目前的测试，即使选择正确，也显示错误，不计算积分。修复这个问题。
6. 我的时间很充裕，所以不需要为我节省时间，尽可能修复所有问题，确保项目的质量。
7. 进入前端目录fronted，执行build.bat, 确保前端构建成功。如果有问题，修复问题
8. 进入后端目录backend，执行build.bat, 确保后端构建成功。如果有问题，修复问题
9. 回到根目录，执行标准的git commit，生成标准的git commit