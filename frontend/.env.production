# Production environment for Go-Study2 Frontend
# Leave NEXT_PUBLIC_API_URL empty to use relative paths (/api/v1)
# This ensures API requests go to the same origin as the frontend

# Empty = use relative path (works with both domain and IP access)
NEXT_PUBLIC_API_URL=
