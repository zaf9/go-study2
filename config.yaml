server:
  # HTTP服务监听地址（必填，无默认值）
  host: "127.0.0.1"
  # HTTP服务监听端口（必填，无默认值）
  port: 8080
  # 优雅关闭超时时间（秒）
  shutdownTimeout: 10

# 日志配置
logger:
  # 日志级别: DEBUG, INFO, WARN, ERROR
  level: "INFO"
  # 日志输出路径
  path: "./logs"
  # 是否输出到控制台
  stdout: true
