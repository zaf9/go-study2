# Quickstart - 后端目录重构与前端预留

## 构建与测试指引

- 优先在仓库根执行 `./build.bat`；若不存在则使用项目通用构建命令（例如 `go build ./...`），并在此记录实际使用的命令和结果。
- 在 `backend/` 下执行 `go test ./...`，记录测试通过情况与异常说明。

## 迁移后验证记录

- 记录迁移后首次构建与测试的执行命令、时间、结果。
- 若有路径或脚本调整，请注明修改点与验证方式。

